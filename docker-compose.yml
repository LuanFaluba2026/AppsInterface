services:
  postgres:
    image: postgres:16
    container_name: suporte-postgres
    environment:
      POSTGRES_DB: suporte_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: luri5246
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: